// placeholder
// you can learn more about api mocking here ...

export const mockApiAppSetup = ({mock, overrides}) => {
  // mocking behavior
}
export const mockApiFeeds = ({mock, overrides}) => {
  // mocking behavior
}
export const mockApiCurrentUser = ({mock, overrides}) => {
  // mocking behavior
}
